/vendor/
/var/
# Stray core directory produced if roots/wordpress-core-installer ever runs
# without extra.wordpress-install-dir (its default install dir). The pinned
# 7.1 core is relocated to /var/wordpress via composer.json extra config.
/wordpress/
/.phpunit.result.cache
/build/

# Disposable local dev environment (untracked; never commit)
/.local/
/docker-compose.local.yml
